import assert from "node:assert/strict"; import test from "node:test"; import { CONTEXT_HANDOFF_COMPATIBILITY_POLICY as policy } from "../src/context-handoff-policy.mjs";
test("P7 supported compatibility policy freezes full handoff versus model-switch semantics",()=>{assert.equal(policy.full_session_handoff,"history-zero-replacement-pi-session");assert.equal(policy.external_context_rebind,"before-resume-ready");assert.equal(policy.resume_ready_telemetry,"after-successful-rebind");assert.equal(policy.rebind_failure,"fail-closed-continuity-failed");assert.equal(policy.model_switch,"same-pi-session-no-full-handoff");});
